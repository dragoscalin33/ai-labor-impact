# Lean runtime — what the dashboard + API + tests need.
# Heavier optional stacks live in:
#   requirements-bayesian.txt  (PyMC, ArviZ — for notebooks/04)
#   requirements-tracking.txt  (MLflow — for src/tracking)
#   requirements-dev.txt       (everything: dev tools, notebooks, optional stacks)

# Core data & analysis
pandas>=2.1.0,<3
numpy>=1.26.0,<2
scipy>=1.11.0
requests>=2.31.0

# Visualization
plotly>=5.18.0

# Dashboard
streamlit>=1.30.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.5.0
